import { I_Article } from "../cms/types.ts";


/**
 * Get articles based on the selected CMS, e.g. Directus, Strapi, etc.
 * 
 * 
 */
export const getArticles = async():Promise<I_Article[]> => {
    const articles:I_Article[] = [];

    return articles;
}